# 안드로이드 앱 빌드 가이드

## ⭐ 방법 1: 현재 폴더를 직접 Android Studio 프로젝트로 변환 (가장 간단!)

**이 방법이 가장 편리합니다!** 다른 곳에서 프로젝트를 만들고 복사할 필요가 없습니다. 현재 폴더에서 바로 시작할 수 있습니다.

### 1단계: 프로젝트 구조 생성

1. **PowerShell 실행**
   - Windows 키 + X → **Windows PowerShell** 선택
   - 또는 시작 메뉴에서 PowerShell 검색

2. **스크립트 실행**
   ```powershell
   cd C:\Users\BSA\AndroidStudioProjects\yellow
   .\프로젝트_구조_생성.ps1
   ```
   
   **⚠️ 스크립트 실행 권한 오류가 발생하는 경우:**
   ```powershell
   # 실행 정책 변경 (한 번만 실행)
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   
   # 그 다음 스크립트 실행
   .\프로젝트_구조_생성.ps1
   ```

3. **완료!**
   - 스크립트가 자동으로 Android Studio 프로젝트 구조를 생성합니다
   - 모든 파일을 올바른 위치로 이동시킵니다
   - 필요한 설정 파일들을 자동으로 생성합니다

### 2단계: Android Studio에서 프로젝트 열기

1. **Android Studio 실행**
2. **File > Open** 선택
3. 현재 폴더 선택 (`C:\Users\BSA\AndroidStudioProjects\yellow`)
4. **OK** 클릭
5. Android Studio가 자동으로 프로젝트를 인식하고 Gradle 동기화를 시작합니다
6. 동기화 완료 후 바로 빌드 및 실행 가능!

**장점:**
- ✅ 복사할 필요 없음
- ✅ 파일 위치를 신경 쓸 필요 없음
- ✅ 한 번의 스크립트 실행으로 완료
- ✅ 현재 폴더에서 바로 작업 가능

---

## 방법 2: 새 프로젝트 생성 후 파일 복사 (다른 위치에 프로젝트를 만들고 싶은 경우)

### 1단계: 프로젝트 생성

1. **Android Studio 실행**
2. **File > New > New Project** 선택
3. **Empty Activity** 템플릿 선택
4. 프로젝트 설정:
   - **Name**: `RGBSensorBLE`
   - **Package name**: `com.example.rgbsensorble`
   - **Save location**: 원하는 위치 선택
   - **Language**: **Kotlin** 선택
   - **Minimum SDK**: **API 21** (Android 5.0) 선택
   - **Build configuration language**: Gradle Kotlin DSL 또는 Groovy 선택
5. **Finish** 클릭

### 2단계: 자동 파일 복사 (스크립트 사용)

1. **PowerShell 실행**
   ```powershell
   cd C:\Users\BSA\AndroidStudioProjects\yellow
   .\자동_파일_복사.ps1
   ```

2. **프로젝트 경로 입력**
   - 새로 만든 Android Studio 프로젝트 경로를 입력하세요
   - 예: `C:\Users\BSA\AndroidStudioProjects\RGBSensorBLE`

3. **완료!**
   - 스크립트가 자동으로 모든 파일을 복사합니다

---

## 방법 3: 수동 파일 복사 (스크립트를 사용할 수 없는 경우)

### 1단계: 프로젝트 생성

1. **Android Studio 실행**
2. **File > New > New Project** 선택
3. **Empty Activity** 템플릿 선택
4. 프로젝트 설정:
   - **Name**: `RGBSensorBLE`
   - **Package name**: `com.example.rgbsensorble`
   - **Save location**: 원하는 위치 선택
   - **Language**: **Kotlin** 선택
   - **Minimum SDK**: **API 21** (Android 5.0) 선택
   - **Build configuration language**: Gradle Kotlin DSL 또는 Groovy 선택
5. **Finish** 클릭

### 2단계: 파일 복사

**중요**: Android Studio에서 새 프로젝트를 만들면 기본 템플릿 파일들이 자동으로 생성됩니다. 이 단계에서는 **현재 소스 코드가 있는 폴더**(예: `yellow` 폴더)에 있는 파일들을 새로 만든 Android Studio 프로젝트로 복사해야 합니다.

생성된 프로젝트 폴더에 다음 파일들을 복사하세요:

#### 소스 파일 위치
현재 소스 코드가 있는 폴더(예: `yellow` 폴더)에서 다음 파일들을 찾아 복사합니다:
- `MainActivity.kt`
- `activity_main.xml`
- `AndroidManifest.xml`
- `build.gradle` (dependencies 부분 확인)

#### 프로젝트 구조
```
RGBSensorBLE/  (새로 만든 Android Studio 프로젝트)
├── app/
│   ├── src/
│   │   └── main/
│   │       ├── java/com/example/rgbsensorble/
│   │       │   └── MainActivity.kt  ← 여기에 복사 (기존 파일 교체)
│   │       ├── res/
│   │       │   └── layout/
│   │       │       └── activity_main.xml  ← 여기에 복사 (기존 파일 교체)
│   │       └── AndroidManifest.xml  ← 여기에 복사 (기존 파일 교체)
│   └── build.gradle  ← dependencies 부분 확인 및 추가
```

#### 복사할 파일들:

**⚠️ 중요: `layout` 폴더가 없는 경우**

만약 `app/src/main/res/layout/` 폴더가 없다면 다음 방법으로 생성하세요:

**방법 1: Android Studio에서 생성 (권장)**
1. Android Studio 왼쪽 **Project** 패널에서 `app/src/main/res` 폴더를 찾습니다
2. `res` 폴더를 **우클릭** → **New** → **Directory** 선택
3. 폴더 이름에 `layout` 입력 후 **OK** 클릭

**방법 2: 파일 탐색기에서 직접 생성**
1. Windows 탐색기에서 프로젝트 폴더 열기
2. `app/src/main/res/` 경로로 이동
3. `layout` 폴더 생성

---

1. **`MainActivity.kt`** → `app/src/main/java/com/example/rgbsensorble/MainActivity.kt` (기존 파일 교체)
2. **`activity_main.xml`** → `app/src/main/res/layout/activity_main.xml` (기존 파일 교체 또는 새로 생성)
3. **`AndroidManifest.xml`** → `app/src/main/AndroidManifest.xml` (기존 파일 교체)
4. **`build.gradle`** → `app/build.gradle`의 dependencies 부분 확인 및 필요한 의존성 추가

**참고**: Android Studio에서 생성된 기본 파일들은 템플릿 파일이므로, 실제 앱 코드로 교체해야 합니다.

### 3단계: Gradle 동기화

1. Android Studio 상단 메뉴에서 **File > Sync Project with Gradle Files** 클릭
2. 또는 오른쪽 상단의 **Sync Now** 버튼 클릭
3. 동기화 완료 대기

### 4단계: 빌드

#### 디버그 APK 빌드 (테스트용)
1. 상단 메뉴: **Build > Make Project** (단축키: `Ctrl+F9` / `Cmd+F9`)
2. 또는 **Build > Build Bundle(s) / APK(s) > Build APK(s)**
3. 빌드 완료 후 하단에 알림 표시
4. **locate** 링크 클릭하여 APK 파일 위치 확인

#### 릴리즈 APK 빌드 (배포용)
1. **Build > Generate Signed Bundle / APK** 선택
2. **APK** 선택 후 **Next**
3. 키스토어 생성 또는 기존 키스토어 선택
4. 서명 정보 입력
5. **release** 빌드 타입 선택
6. **Finish** 클릭

### 5단계: APK 설치

#### 방법 A: USB 디버깅
1. 안드로이드 기기에서 **개발자 옵션** 활성화
2. **USB 디버깅** 활성화
3. USB 케이블로 PC와 연결
4. Android Studio에서 **Run** 버튼 클릭 (단축키: `Shift+F10`)
5. 연결된 기기 선택 후 실행

#### 방법 B: APK 파일 직접 설치
1. 빌드된 APK 파일을 안드로이드 기기로 전송
2. 기기에서 **설정 > 보안 > 알 수 없는 소스** 허용
3. APK 파일 실행하여 설치

---

## 방법 4: 명령줄을 사용한 빌드

### 사전 준비

1. **Android SDK 설치** (Android Studio와 함께 설치됨)
2. **환경 변수 설정**:
   - `ANDROID_HOME`: Android SDK 경로
   - `PATH`에 추가: `$ANDROID_HOME/platform-tools`, `$ANDROID_HOME/tools`

### Windows PowerShell에서 빌드

```powershell
# 프로젝트 폴더로 이동
cd D:\path\to\RGBSensorBLE

# Gradle 래퍼를 사용한 빌드
.\gradlew.bat assembleDebug

# 또는 릴리즈 빌드
.\gradlew.bat assembleRelease
```

### Linux/Mac 터미널에서 빌드

```bash
# 프로젝트 폴더로 이동
cd /path/to/RGBSensorBLE

# Gradle 래퍼를 사용한 빌드
./gradlew assembleDebug

# 또는 릴리즈 빌드
./gradlew assembleRelease
```

### APK 파일 위치

빌드된 APK 파일은 다음 위치에 생성됩니다:
```
app/build/outputs/apk/debug/app-debug.apk  (디버그)
app/build/outputs/apk/release/app-release.apk  (릴리즈)
```

---

## 방법 5: Android Studio에서 직접 실행

### 1단계: 기기 연결

1. 안드로이드 기기에서 **개발자 옵션** 활성화
2. **USB 디버깅** 활성화
3. USB 케이블로 PC와 연결
4. 기기에서 "USB 디버깅 허용" 확인

### 2단계: 실행

1. Android Studio 상단 툴바에서 **Run** 버튼 클릭 (녹색 재생 아이콘)
2. 또는 **Run > Run 'app'** (단축키: `Shift+F10`)
3. 연결된 기기 선택
4. 앱이 자동으로 빌드되고 설치됨

---

## 빌드 문제 해결

### 오류: "SDK location not found"
- **File > Project Structure > SDK Location**에서 Android SDK 경로 확인

### 오류: "Gradle sync failed"
- **File > Invalidate Caches / Restart** 실행
- **File > Sync Project with Gradle Files** 다시 실행

### 오류: "Build failed"
- **Build > Clean Project** 실행 후 다시 빌드
- **Build > Rebuild Project** 실행

### 오류: "Permission denied"
- `AndroidManifest.xml`에 필요한 권한이 모두 포함되어 있는지 확인

### 오류: "Device not found"
- USB 디버깅이 활성화되어 있는지 확인
- USB 드라이버 설치 확인 (Windows)
- `adb devices` 명령어로 기기 인식 확인

### 오류: "Unresolved reference 'layout'" 또는 "Unresolved reference 'R'"
이 오류는 리소스 파일이 제대로 인식되지 않을 때 발생합니다. 다음 순서로 해결하세요:

**🔍 빠른 진단:**
1. Windows 탐색기에서 `C:\Users\BSA\AndroidStudioProjects\RGBSensorBLE\app\src\main\res\layout\activity_main.xml` 파일이 있는지 확인
2. Android Studio의 Project 뷰에서 `app > src > main > res > layout > activity_main.xml`이 보이는지 확인

**✅ 해결 방법 (순서대로 시도):**

1. **Gradle 동기화 (가장 먼저!)**
   - **File > Sync Project with Gradle Files** 실행
   - 또는 오른쪽 상단의 **Sync Now** 버튼 클릭
   - 동기화 완료 대기

2. **파일 위치 확인 및 복사 (파일이 없는 경우)**
   - Windows 탐색기에서 `C:\Users\BSA\AndroidStudioProjects\yellow\activity_main.xml` 복사
   - `C:\Users\BSA\AndroidStudioProjects\RGBSensorBLE\app\src\main\res\layout\` 폴더에 붙여넣기
   - `layout` 폴더가 없으면 먼저 생성

3. **프로젝트 정리 및 재빌드**
   - **Build > Clean Project** 실행
   - 완료 후 **Build > Rebuild Project** 실행

4. **캐시 무효화**
   - **File > Invalidate Caches / Restart** 선택
   - **Invalidate and Restart** 클릭
   - Android Studio 재시작 후 Gradle 동기화

5. **build.gradle 확인**
   - `app/build.gradle` 파일 열기
   - `namespace 'com.example.rgbsensorble'`이 있는지 확인 (없으면 추가)

6. **패키지 확인**
   - `MainActivity.kt`의 첫 줄이 `package com.example.rgbsensorble`인지 확인
   - `build.gradle`의 `namespace`와 일치해야 함

**💡 자세한 해결 가이드는 `문제_해결_가이드.md` 파일을 참고하세요.**

---

## 빌드 타입 설명

### Debug (디버그)
- 개발 및 테스트용
- 서명 자동 생성
- 최적화 없음
- 빠른 빌드

### Release (릴리즈)
- 배포용
- 코드 난독화 및 최적화
- 서명 필요 (키스토어)
- 작은 파일 크기

---

## APK 파일 크기 최적화

### ProGuard 활성화
`app/build.gradle`에서:
```gradle
buildTypes {
    release {
        minifyEnabled true
        proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
    }
}
```

### 불필요한 리소스 제거
```gradle
android {
    buildTypes {
        release {
            shrinkResources true
            minifyEnabled true
        }
    }
}
```

---

## 빠른 체크리스트

- [ ] Android Studio 설치 완료
- [ ] 프로젝트 생성 완료
- [ ] 모든 파일 복사 완료
- [ ] Gradle 동기화 완료
- [ ] 빌드 오류 없음
- [ ] 안드로이드 기기 연결됨
- [ ] USB 디버깅 활성화됨
- [ ] 앱 설치 및 실행 성공


